exports.processors =
{
    txt:
    {
        preprocess(text)
        {
            return [text];
        },
        postprocess(messages)
        {
            return messages[0];
        },
    },
};
