exports.processors =
{
    txt:
    {
        preprocess(text)
        {
            return [text.replace('a()', 'b()')];
        },
        postprocess(messages)
        {
            messages[0][0].ruleId = 'post-processed';
            return messages[0];
        },
    },
};
